@page "/Error/{statusCode:int}"
@model AceJobAgency.Pages.HttpErrorModel
@{
    ViewData["Title"] = Model.Title;
}

<h1>@Model.Title</h1>
<p class="text-muted">Status code: @Model.HttpStatusCode</p>
<p>@Model.Message</p>
<a asp-page="/Index" class="btn btn-primary">Back to Home</a>
